{% extends '/admin/base.html.twig' %}

{% block title %}Edit Post{% endblock %}

{% block content %}
    {{ parent() }}
    <a class="btn h-btn-outline-success mb-sm-3"  href="{{ path('section_index') }}">{{ 'global.back_to_list_section'|trans()|raw }}</a>
    <a class="btn h-btn-outline-success mb-sm-3"  href="{{ path('post_index') }}">{{ 'global.back_to_list_content'|trans()|raw }}</a>

    {{ include('admin/post/_form.html.twig', {'button_label': 'global.update'|trans()}) }}

    {{ include('admin/post/_delete_form.html.twig') }}

    <a class="btn h-btn-outline-success mb-sm-3"  href="{{ path('section_index') }}">{{ 'global.back_to_list_section'|trans()|raw }}</a>
    <a class="btn h-btn-outline-success mb-sm-3"  href="{{ path('post_index') }}">{{ 'global.back_to_list_content'|trans()|raw }}</a>

{% endblock %}
